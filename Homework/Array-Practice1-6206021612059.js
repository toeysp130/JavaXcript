let feuniture = ['Table','Charis','Couch'];

    let i = 0
    while(i < feuniture.length){
        const name = feuniture[i];
        console.log(name);
        i++
        for(let char of name){
            console.log(char);
            
        }
    }
